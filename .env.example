# Copy to .env for local dev, or maintain /etc/rent-split.env on the host
# (chmod 600). Production (Docker Compose) overrides PORT / DATA_DIR /
# PUBLIC_DIR inside the container. See docker-compose.prod.yml.

# HTTP port the FastAPI server listens on.
PORT=8080

# Browser tab / shell brand for this instance (not stored in SQLite).
SITE_TITLE=Rent Split

# Where rent-split.db, backups/, and optional legacy JSON live.
# Local default: ./data at the repo root.
# Host path example (bind-mounted): /opt/rent-split-site/data
# Inside the production container: /data (set by compose).
DATA_DIR=

# Directory of built frontend assets (defaults to ../frontend/dist from server/).
# Production image sets PUBLIC_DIR=/app/public.
PUBLIC_DIR=
